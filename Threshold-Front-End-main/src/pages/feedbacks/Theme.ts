import styled from 'styled-components';
import { Button as DefaultButton, Text as DefaultText } from 'components';

export const Body = styled.div.attrs({
    className: 'w-full flex flex-col pt-[50px] gap-[20px]',
})``;
